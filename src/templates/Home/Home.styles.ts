import styled from '@emotion/styled';

export const MainContainer = styled('main')`
  display: flex;
  flex-direction: column;
  gap: 20px;
  alignItems: center;
  padding: 6rem 1rem;
  minHeight: 100vh;
`;